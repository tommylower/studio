# house — the earned state

this install's use of the studio system: invariants, defaults, running
experiments, resolved history. graded by the ladder in `rules.md`
(experiment → default → invariant). gitignored: rows earned here never
ship with the tool.

## invariants — ten slots, forever

nothing enters without surviving two real uses. borrowed rules enter the
experiments table below, not here.

1.

## defaults — current, swappable, no shame in changing

| area | current default | why / provenance |
|---|---|---|
| | | |

## experiments — running, free to kill

one row per deposit. format, by example:

| experiment | where | verdict due |
|---|---|---|
| figma plugin api: wrapping TEXT in auto-layout gets textAutoResize='HEIGHT' + FIXED width via resize(), never FILL alone (FILL can resolve w=0 → node explodes to a 4000px vertical thread) | 2026-07-04 project-a gap-pass, cost two debug rounds | after next figma build session |

## resolved

promotions, merges, and kills, dated, one line each.
